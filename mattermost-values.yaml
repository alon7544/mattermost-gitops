# ערוך את mattermost-values.yaml
postgresql:
  enabled: true
  # בגרסה הזו של ה-Chart, אלו השדות הנכונים:
  postgresqlUsername: mmuser
  postgresqlPassword: password123
  postgresqlDatabase: mattermost
  persistence:
    enabled: false
  primary:
    persistence:
      enabled: false

# ביטול מסדי נתונים אחרים
mysql:
  enabled: false
externalDB:
  enabled: false
